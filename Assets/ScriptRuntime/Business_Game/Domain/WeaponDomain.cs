using UnityEngine;

public static class WeaponDomain {

    // public static void Spawn(GameContext ctx, int typeID, Transform trans) {
    //     var weapon = Factory.Weapon_Spawn(ctx, typeID, trans);
    //     ctx.GetOwner().weaponCom.Add(weapon);
    // }
}